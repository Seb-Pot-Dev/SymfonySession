{% extends 'base.html.twig' %}

{% block title %}Hello StudentController!{% endblock %}

{% block body %}
<h2>Ajouter un étudiant </h2>
{{ form(formAddStudent)}}
<h1>Liste des étudiants </h1>
{% if students|length >= 1 %}
<table>
    <thead class="table">
        <tr>
            <th colspan="2">Liste des étudiants</th>
        </tr>
        <tr>
            <td>Nom</td>
            <td>Action</td>

        </tr>
    </thead>
    <tbody class="table">
        {% for student in students %}
            <tr class="clickable-row" 
            {# onclick="window.location='{{ path('show_student', {'id': student.id}) }}'" #}
            >
                <td >{{ student }}</td>
                <td><a class="button-delete" href="{{ path('remove_student', {'id': student.id}) }}">suppr</a></td>
            </tr>
        {% endfor %}
    </tbody>
</table>
{% else %}
<p>Aucun étudiants enregistrés.</p>
{% endif %}
{% endblock %}
